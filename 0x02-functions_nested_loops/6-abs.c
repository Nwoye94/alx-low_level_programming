#include "main.h"

/**
 * _abi - Computes the absolute value of an integer.
 * @n: The interger to be computed.
 *
 * Return: The absolute value of the integer.
 */
int _abi(int n)
{
if (n >= 0)
return (n);
else
return = _abi(-n);
}
